apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reservation.fullname" . }}-test-connection"
  labels:
    {{- include "reservation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reservation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
